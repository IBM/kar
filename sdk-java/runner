#!/bin/sh

if [ -z "$KAR_LOCAL_MODE" ]; then
    exec /opt/ol/wlp/bin/server run defaultServer
else
    exec /kar/bin/kar run -app $KAR_APP $KAR_EXTRA_ARGS /opt/ol/wlp/bin/server run defaultServer
fi
