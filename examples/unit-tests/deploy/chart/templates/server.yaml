apiVersion: apps/v1
kind: Deployment
metadata:
  name: ut-server
  labels:
    name: ut-server
spec:
  replicas: 2
  selector:
    matchLabels:
      name: ut-server
  template:
    metadata:
      labels:
        name: ut-server
      annotations:
        kar.ibm.com/app: myApp
        kar.ibm.com/service: myService
        kar.ibm.com/actors: Foo
        kar.ibm.com/verbose: info
    spec:
      containers:
      - name: server
        image: {{ .Values.image }}
        env:
        - name: MAIN
          value: unit-tests/server.js
