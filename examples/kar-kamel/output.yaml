- from:
    uri: "kafka:OutputStockEvent"
    steps:
      - process:
          ref: "transformCloudEventToMessage"
      - filter:
          expression:
            simple: "${in.header.redirectToSlack} == 'true'"
          steps:
            - to: "slack:#kar-output"
      - log:
          message: "${body}"
