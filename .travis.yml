language: go
services:
  - docker

go:
  - 1.13

script:
  - ./build/ci/build.sh

deploy:
  - provider: script
    script: "./build/ci/pushImages.sh ${TRAVIS_TAG}"
    on:
      tags: true
      all_branches: true
      repo: solsa/kar
  - provider: script
    script: "./build/ci/pushImages.sh nightly"
    on:
      branch: master
      repo: solsa/kar
